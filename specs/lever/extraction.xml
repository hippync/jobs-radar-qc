<?xml version="1.0" encoding="UTF-8"?>
<!--
  Lever extraction rules.

  Key differences from Greenhouse:
  - Response is a bare array (jobs_path=null in schema.json)
  - createdAt is Unix milliseconds → use type="unix_ms"
  - workplaceType provides a structured is_remote signal
  - categories.commitment provides employment_type directly
  - description is the main body; lists[] holds requirement/responsibility sections
    → concat_array="lists" appends all list content to description_html so that
      deterministic keyword matching AND LLM enrichment both see the full posting.

  _workplace_type and _commitment are intermediate fields — they are
  used by derived rules but ignored by the storage layer.
-->
<extraction>

  <constants>
    <constant name="source"    value="lever" />
    <constant name="is_active" value="true"  type="bool" />
  </constants>

  <fields>
    <field name="external_id"      from="id"                    type="string"   />
    <field name="title"            from="text"                  type="string"   />
    <field name="source_url"       from="hostedUrl"             type="string"   />
    <field name="location"         from="categories.location"   type="string" />
    <field name="department"       from="categories.department" type="string" />
    <!--
      concat_array="lists" array_field="content": appends the HTML content of every
      list section (e.g. "What we're looking for") to the main description body.
      Without this, technologies mentioned only in requirements lists are missed.
    -->
    <field name="description_html" from="description"           type="string"
           empty_as_null="true"
           concat_array="lists" array_field="content" />
    <field name="posted_at"        from="createdAt"             type="unix_ms" />

    <!-- Intermediate fields for derivation — not written to DB -->
    <field name="_workplace_type"  from="workplaceType"              type="string" />
    <field name="_commitment"      from="categories.commitment"      type="string" />
  </fields>

  <derived>

    <field name="is_qc" type="bool" default="false">
      <match source="location"
             pattern="(?i)(montr[eé]al|qu[eé]bec|laval|longueuil|brossard|sherbrooke|gatineau|\bqc\b)"
             value="true" />
    </field>

    <!-- workplaceType is the authoritative signal; fall back to location string -->
    <field name="is_remote" type="bool" default="null">
      <match source="_workplace_type" pattern="(?i)^remote$"   value="true"  />
      <match source="_workplace_type" pattern="(?i)^hybrid$"   value="null"  />
      <match source="_workplace_type" pattern="(?i)^on.?site$" value="false" />
      <match source="location"        pattern="(?i)\bremote\b" value="true"  />
    </field>

    <!-- employment_type: normalize Lever commitment values to canonical enum -->
    <field name="employment_type" type="string" default="null">
      <match source="_commitment" pattern="(?i)full.?time"                          value="full-time"   />
      <match source="_commitment" pattern="(?i)part.?time"                          value="part-time"   />
      <match source="_commitment" pattern="(?i)(contract|consultant|fixed.?term)"   value="contract"    />
      <match source="_commitment" pattern="(?i)(intern|stage|stagiaire)"            value="internship"  />
    </field>

    <field name="seniority" type="string" default="null">
      <match source="title" pattern="(?i)\b(intern|stage|stagiaire|internship)\b"          value="internship" />
      <match source="title" pattern="(?i)\b(junior|jr\.?|d[eé]butant)\b"                   value="junior"     />
      <match source="title" pattern="(?i)\b(staff)\b"                                      value="staff"      />
      <match source="title" pattern="(?i)\b(principal)\b"                                  value="principal"  />
      <match source="title" pattern="(?i)\b(tech(nical)?\s+lead|lead\s+(dev|engineer))\b"  value="lead"       />
      <match source="title" pattern="(?i)\b(senior|sr\.?|exp[eé]riment[eé])\b"             value="senior"     />
      <match source="title" pattern="(?i)\b(director|directeur)\b"                         value="director"   />
      <match source="title" pattern="(?i)\b(manager|gestionnaire)\b"                       value="manager"    />
    </field>

    <!--
      tech_stack: collect ALL keyword matches across title and description_html.

      word_boundary="true" is implemented by the extractor: adds \b at word-char
      positions of each pattern, preventing substring false positives:
        - Java  → \bJava\b  (no match inside "JavaScript")
        - Rust  → \bRust\b  (no match inside "trust", "frustrating")
        - Vue   → \bVue\b   (no match inside "Entrevue" — French for interview)
        - Scala → \bScala\b (no match inside "scalability")

      HTML is stripped from source fields before matching to prevent false positives
      from tech names appearing in HTML attributes (e.g. data-path-to-node → Node.js).

      case_sensitive="true" on individual <kw> overrides the element-wide flag for
      ambiguous short tokens (Go, REST, LLM, RAG, R) to prevent case-folded matches.
    -->
    <field name="tech_stack" type="list[string]" default="[]" strategy="collect-all">
      <keywords sources="title,description_html" word_boundary="true" case_insensitive="true">

        <!-- Languages -->
        <kw canonical="Python">Python</kw>
        <kw canonical="JavaScript">JavaScript</kw>
        <kw canonical="TypeScript">TypeScript</kw>
        <!-- Go: case_sensitive prevents matching "google", "agora", etc. -->
        <kw canonical="Go" case_sensitive="true">\b(?:Go|Golang)\b</kw>
        <kw canonical="Rust">Rust</kw>
        <kw canonical="Java">Java</kw>
        <kw canonical="Kotlin">Kotlin</kw>
        <kw canonical="Scala">Scala</kw>
        <kw canonical="C#">C#</kw>
        <kw canonical="C++">C\+\+</kw>
        <kw canonical="Ruby">Ruby</kw>
        <kw canonical="PHP">PHP</kw>
        <kw canonical="Swift">Swift</kw>
        <kw canonical="Dart">Dart</kw>
        <kw canonical="Elixir">Elixir</kw>
        <kw canonical="Erlang">Erlang</kw>
        <!-- R: explicit \b guards since it is a single letter; case_sensitive avoids
             matching the letter r in French/English words. -->
        <kw canonical="R" case_sensitive="true">\bR\b</kw>
        <!-- SQL: case_sensitive + \b prevents matching MySQL / PostgreSQL -->
        <kw canonical="SQL" case_sensitive="true">\bSQL\b</kw>

        <!-- Frontend / Web -->
        <kw canonical="React">React</kw>
        <kw canonical="Next.js">Next\.js</kw>
        <kw canonical="Vue">Vue(?:\.js)?</kw>
        <kw canonical="Angular">Angular</kw>
        <kw canonical="Svelte">Svelte</kw>
        <kw canonical="Node.js">Node(?:\.js)?</kw>
        <kw canonical="Express">Express</kw>
        <kw canonical="GraphQL">GraphQL</kw>
        <!-- REST: case_sensitive prevents "rest period", "at rest", etc. -->
        <kw canonical="REST" case_sensitive="true">REST(?:ful)?</kw>
        <kw canonical="Tailwind">Tailwind(?:\s+CSS)?</kw>
        <kw canonical="Redux">Redux</kw>
        <!-- Vite: case_sensitive avoids matching French "vite" (quickly) in bilingual postings -->
        <kw canonical="Vite" case_sensitive="true">Vite</kw>
        <kw canonical="tRPC" case_sensitive="true">tRPC</kw>
        <kw canonical="Remix">Remix</kw>
        <kw canonical="Storybook">Storybook</kw>

        <!-- Backend frameworks -->
        <kw canonical="Django">Django</kw>
        <kw canonical="FastAPI">FastAPI</kw>
        <kw canonical="Flask">Flask</kw>
        <kw canonical="Rails">(?:Ruby on )?Rails</kw>
        <!-- Spring(?:\s+Boot)? matches both "Spring" and "Spring Boot";
             canonical is "Spring Boot" to align with canonical_tech_stack.json v2 -->
        <kw canonical="Spring Boot">Spring(?:\s+Boot)?</kw>
        <kw canonical="NestJS">NestJS</kw>
        <kw canonical="gRPC">gRPC</kw>
        <kw canonical="Celery">Celery</kw>
        <kw canonical="Laravel">Laravel</kw>
        <kw canonical="ASP.NET">ASP\.NET</kw>
        <kw canonical=".NET">\.NET</kw>
        <!-- Gin: case_sensitive avoids matching the common English noun -->
        <kw canonical="Gin" case_sensitive="true">Gin</kw>

        <!-- Data / ML / AI -->
        <kw canonical="Spark">(?:Apache )?Spark</kw>
        <kw canonical="Kafka">(?:Apache )?Kafka</kw>
        <kw canonical="Airflow">(?:Apache )?Airflow</kw>
        <kw canonical="Flink">(?:Apache )?Flink</kw>
        <kw canonical="dbt">dbt</kw>
        <kw canonical="Pandas">Pandas</kw>
        <kw canonical="NumPy">NumPy</kw>
        <kw canonical="PyTorch">PyTorch</kw>
        <kw canonical="TensorFlow">TensorFlow</kw>
        <kw canonical="scikit-learn">scikit-learn</kw>
        <kw canonical="Databricks">Databricks</kw>
        <kw canonical="RabbitMQ">RabbitMQ</kw>
        <!-- LLM/RAG: case_sensitive avoids "llm"/"rag" as lowercase abbreviations -->
        <kw canonical="LLM" case_sensitive="true">LLM</kw>
        <kw canonical="RAG" case_sensitive="true">RAG</kw>
        <kw canonical="LangChain">LangChain</kw>
        <!-- OpenAI: case_sensitive matches the brand name precisely -->
        <kw canonical="OpenAI" case_sensitive="true">OpenAI</kw>
        <kw canonical="MLflow">MLflow</kw>
        <!-- Vector DB: matches named databases + plain-English descriptions -->
        <kw canonical="Vector DB">(?:Pinecone|Qdrant|Weaviate|Chroma|vector\s+(?:database|store|DB))</kw>
        <kw canonical="Hugging Face">(?:Hugging\s+Face|HuggingFace)</kw>
        <kw canonical="Weights &amp; Biases">Weights\s+(?:&amp;|and)\s+Biases</kw>

        <!-- Infrastructure / Cloud -->
        <kw canonical="AWS">AWS</kw>
        <kw canonical="GCP">GCP</kw>
        <kw canonical="Azure">Azure</kw>
        <kw canonical="Vercel">Vercel</kw>
        <kw canonical="DigitalOcean">DigitalOcean</kw>
        <kw canonical="Heroku">Heroku</kw>
        <kw canonical="Kubernetes">Kubernetes</kw>
        <kw canonical="Docker">Docker</kw>
        <kw canonical="Terraform">Terraform</kw>
        <kw canonical="Ansible">Ansible</kw>
        <kw canonical="CI/CD">CI/CD</kw>
        <kw canonical="GitHub Actions">GitHub Actions</kw>
        <kw canonical="GitLab CI">GitLab\s+CI(?:/CD)?</kw>
        <kw canonical="CircleCI">CircleCI</kw>
        <kw canonical="Jenkins">Jenkins</kw>
        <kw canonical="Helm">Helm</kw>
        <!-- ArgoCD: Argo\s*CD matches "ArgoCD" (0 spaces) and "Argo CD" (1 space) -->
        <kw canonical="ArgoCD">Argo\s*CD</kw>
        <kw canonical="Pulumi">Pulumi</kw>
        <kw canonical="Prometheus">Prometheus</kw>
        <kw canonical="Grafana">Grafana</kw>
        <kw canonical="Nginx">Nginx</kw>
        <kw canonical="Cloudflare">Cloudflare</kw>
        <!-- Vault: case_sensitive avoids matching lowercase "vault" (common English noun) -->
        <kw canonical="Vault" case_sensitive="true">(?:HashiCorp\s+)?Vault</kw>

        <!-- Databases -->
        <kw canonical="PostgreSQL">Postgres(?:QL)?</kw>
        <kw canonical="MySQL">MySQL</kw>
        <kw canonical="MongoDB">MongoDB</kw>
        <kw canonical="Redis">Redis</kw>
        <kw canonical="Elasticsearch">Elasticsearch</kw>
        <kw canonical="Snowflake">Snowflake</kw>
        <kw canonical="BigQuery">BigQuery</kw>
        <kw canonical="DynamoDB">DynamoDB</kw>
        <kw canonical="Cassandra">Cassandra</kw>
        <kw canonical="ClickHouse">ClickHouse</kw>
        <kw canonical="Firebase">Firebase</kw>
        <kw canonical="Supabase">Supabase</kw>
        <kw canonical="SQLite">SQLite</kw>

        <!-- Mobile -->
        <kw canonical="React Native">React Native</kw>
        <kw canonical="Flutter">Flutter</kw>
        <kw canonical="SwiftUI">SwiftUI</kw>
        <kw canonical="Android">Android</kw>
        <kw canonical="iOS">iOS</kw>

        <!-- Testing / QA -->
        <kw canonical="Jest">Jest</kw>
        <kw canonical="Vitest">Vitest</kw>
        <kw canonical="Cypress">Cypress</kw>
        <kw canonical="Playwright">Playwright</kw>
        <kw canonical="Selenium">Selenium</kw>
        <kw canonical="JUnit">JUnit</kw>
        <kw canonical="pytest">pytest</kw>
        <kw canonical="Mocha">Mocha</kw>
        <kw canonical="RSpec">RSpec</kw>
        <kw canonical="Postman">Postman</kw>

        <!-- AI Concepts -->
        <kw canonical="AI Agents">(?:AI\s+agents?|agentic(?:\s+AI)?|agents?\s+IA)</kw>
        <kw canonical="Prompt Engineering">(?:prompt\s+engineering|ing[eé]nierie\s+(?:de\s+)?prompts?)</kw>
        <kw canonical="Fine-tuning">(?:fine[-\s]?tuning|finetuning)</kw>
        <kw canonical="Embeddings">(?:vector\s+)?embeddings</kw>
        <kw canonical="Semantic Search">(?:semantic\s+search|recherche\s+s[eé]mantique)</kw>
        <!-- NLP: case_sensitive for the acronym; second entry covers the full phrase -->
        <kw canonical="NLP" case_sensitive="true">NLP</kw>
        <kw canonical="NLP">(?:natural\s+language\s+processing|traitement\s+du\s+langage\s+naturel)</kw>
        <kw canonical="Computer Vision">(?:computer\s+vision|vision\s+par\s+ordinateur)</kw>
        <kw canonical="GenAI">(?:GenAI|generative\s+AI|IA\s+g[eé]n[eé]rative)</kw>

      </keywords>
    </field>

  </derived>

</extraction>
